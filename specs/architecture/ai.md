<!-- mentor:file
The AI integration architecture.
priority: high
-->

# AI Architecture

Not applicable — this product has no AI features.

`@phosphor-icons/react` icons are resolved dynamically by name string in MDX components, but this is simple string-to-component mapping, not AI.
